{
    "name": "refi-dapp",
    "version": "1.0.0",
    "description": "Custom Celo Composer project.",
    "private": true,
    "author": "Celo",
    "license": "MIT",
    "scripts": {
        "react-app:dev": "yarn workspace @refi-dapp/react-app dev",
        "react-app:build": "yarn workspace @refi-dapp/react-app build",
        "react-app:start": "yarn workspace @refi-dapp/react-app start",
        "react-app:lint": "yarn workspace @refi-dapp/react-app lint",
        "hardhat:test": "yarn workspace @refi-dapp/hardhat test",
        "hardhat:test-local": "yarn workspace @refi-dapp/hardhat test-local",
        "hardhat:accounts": "yarn workspace @refi-dapp/hardhat accounts",
        "hardhat:devchain": "yarn workspace @refi-dapp/hardhat devchain",
        "hardhat:deploy": "yarn workspace @refi-dapp/hardhat deploy",
        "hardhat:deploy-reset-watch": "yarn workspace @refi-dapp/hardhat deploy-reset-watch",
        "hardhat:watch": "yarn workspace @refi-dapp/hardhat watch",
        "hardhat:fork-mainnet": "yarn workspace @refi-dapp/hardhat fork-mainnet"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/celo-org/celo-composer.git"
    },
    "bugs": {
        "url": "https://github.com/celo-org/celo-composer/issues"
    },
    "homepage": "https://github.com/celo-org/celo-composer/blob/main/README.md",
    "workspaces": [
        "packages/*"
    ],
    "keywords": [
        "celo-composer",
        "celo"
    ]
}
